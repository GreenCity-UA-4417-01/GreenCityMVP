package greencity.repository.event;

import greencity.entity.event.EventImageData;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EventImageDataRepository extends JpaRepository<EventImageData, Long> {
}
